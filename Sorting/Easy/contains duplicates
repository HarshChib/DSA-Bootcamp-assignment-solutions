class Solution {
    public boolean containsDuplicate(int[] nums) {
        int n=nums.length;
      Arrays.sort(nums);
       for(int i=0;i<n-1;i++){
           if(nums[i]==nums[i+1])
               return true;
       }
                return false;
            }

    
 static void swap(int []arr,int i,int j){
        int temp=arr[i];
        arr[i]=arr[j];
        arr[j]=temp;
    }
}